#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <limits>

int main() {
    std::cout << "C++ Array Practice Problems\n";
    std::cout << "---------------------------\n\n";

    // Problem 1: Array Declaration and Initialization
    // Declare an integer array named 'numbers' with a size of 5.
    // Initialize the array with the values 10, 20, 30, 40, and 50.

    // Problem 2: Accessing and Printing Array Elements
    // Write C++ code to print the second element (value 20) of the 'numbers' array to the console.

    // Problem 3: Modifying Array Elements
    // Write C++ code to change the third element of the array 'numbers' to 100, and then print the updated third element to the console.

    // Problem 4: Sum of Array Elements
    // Write C++ code to calculate the sum of all the elements in the 'numbers' array, and print the sum.

    // Problem 5: Finding the Largest Element
    // Write C++ code to find the largest element in the 'numbers' array, and print the largest element to the console.

    // Problem 6: Array Reversal
    // Write C++ code to reverse the order of the elements in the 'numbers' array, and then print the reversed array to the console.

    // Problem 7: Copying an Array
    // Write C++ code to copy the elements of the 'numbers' array into a new array called 'copiedNumbers', and print the copied array.

    // Problem 8: Even numbers in an array
    // Write C++ code to count the number of even numbers in the \'numbers\' array

    // Problem 9: Product of array elements
    // Write C++ code to calculate the product of all the elements in the \'numbers\' array

    // Problem 10: Check if array is sorted
    // Write C++ code to check if the \'numbers\' array is sorted in ascending order

    return 0;
}