#include <iostream>
#include <string>
#include <iomanip> // Include iomanip for formatting

using namespace std;

/*
**Instructions for Class:**

This C++ program demonstrates a simple text adventure game. 

**Learning Objectives:**

1. Understand and apply conditional statements: `if`, `else if`, and `else`.
2. Learn to use input/output streams (`cin`, `cout`) for user interaction.
3. Gain experience with basic string manipulation in C++.
4. (Extra Credit) Learn to use the `iomanip` library for output formatting.
5. (Extra Credit) Incorporate basic calculations into the game.

**Example Code:**

```cpp
int main() {

  cout << "\n\n" << setw(30) << setfill('*') << " Welcome to the Text Adventure Game! " << setfill(' ') << endl; 
  cout << "You find yourself in a dark cave." << endl;
  cout << "You can either go 'left' or 'right'." << endl;

  string direction;
  cin >> direction;

  if (direction == "left") { 
    cout << "\nYou enter a narrow passageway. " << endl;
    cout << "You encounter a small, glowing mushroom." << endl; 
    // ... further choices and consequences ... 
  } else if (direction == "right") { 
    cout << "\nYou enter a large chamber." << endl;
    cout << "You see a sparkling treasure chest." << endl;
    // ... further choices and consequences ... 
  } else { 
    cout << "\nInvalid choice. Please enter 'left' or 'right'." << endl; 
  }

  return 0;
}
*/

int main() {

    // add code here
    return 0;
}