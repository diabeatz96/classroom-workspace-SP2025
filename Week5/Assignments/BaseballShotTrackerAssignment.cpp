#include <iostream>

using namespace std;

int main() {
    // Basketball Shot Tracker

    // --- Instructions ---

    // 1. Declare two integer variables:
    //    - 'shotsMade' and initialize it to 0.
    //    - 'shotsMissed' and initialize it to 0.

    // 2. Prompt the user to enter the number of shot attempts (an integer).
    //    - Store the input in an integer variable 'numAttempts'.

    // 3. Create a 'for' loop that iterates 'numAttempts' times.
    //    - Inside the loop:
    //      - Prompt the user to enter '1' if the shot was made or '0' if it was missed.
    //      - Store the input in an integer variable 'shotResult'.
    //      - If 'shotResult' is 1, increment 'shotsMade'.
    //      - If 'shotResult' is 0, increment 'shotsMissed'.
    //      - Print a message "Attempt [attempt number]: Made" or "Attempt [attempt number]: Missed" accordingly.

    // 4. After the loop finishes, print the total shots made and missed using 'cout' like:
    //    - "Shots made: [shotsMade]"
    //    - "Shots missed: [shotsMissed]"

    // --- End of Instructions ---

    return 0;
}